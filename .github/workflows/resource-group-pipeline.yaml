name: Module test
on:
  push:
    paths: "resource-group/**"

jobs:
  static_analysis:
    runs-on: ubuntu-latest

    steps:
      - name: Static Analysis
        uses: "./.github/templates/static-analysis.yaml"
        with:
          module: resource-group
          terraform_version: 1.3.6
